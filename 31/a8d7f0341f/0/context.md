# Session Context

## User Prompts

### Prompt 1

some general complaints looking to do a /skillserver jason refactor to address. 

player speed is not carried over on the next page.
nav at the top of the reader disappears even if settings are open
still see white background on theme selector light theme previews(rather than transparent)
player does not keep speed preference
clicking/tapping on any word instantly moves the player, should really be a menu kindle style (user long presses word that opens a menu, menu has a play button, user und...

### Prompt 2

Base directory for this skill: /home/noah/.claude/skills/skillserver

# Skillserver Bridge

Interact with the MCP skillserver (served over LiteLLM gateway on `pylon:4100`)
to list, search, and invoke skills hosted there. Works from any project —
requires only the `litellm-gateway` MCP server to be connected.

## Setup: load the MCP tools

The skillserver MCP tools are deferred. Before doing anything else, load them:

```
ToolSearch query: "+skillserver"
```

This exposes:

- `mcp__litellm-gat...

### Prompt 3

approved

### Prompt 4

approved

### Prompt 5

you can address everything recommended and i trust your instincts on the ones that need direction. feel free to address the other itches too

### Prompt 6

commit push deploy and you can use browser tools to verify

